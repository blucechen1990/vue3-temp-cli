
const downloadFromRepo = require('./downloadFromRepo')
const {getCurrentDirPath} = require('./util')
const installDep = require('./installDep')

module.exports = {
    downloadFromRepo,
    getCurrentDirPath,
    installDep,
}